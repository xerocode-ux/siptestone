"use client";
import {FormEvent, useState} from 'react';
import Link from 'next/link';

export default function KycPage(){const [sent,setSent]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();setSent(true)};return <main className="shell"><div className="topbar"><Link href="/" className="brand">KYC Verify</Link><Link href="/dashboard" className="muted">My dashboard</Link></div><section className="card pad" style={{maxWidth:700,margin:'0 auto'}}><div className="muted">Step 1 of 1</div><h1>Identity verification</h1>{sent?<><div className="notice">Demo submission created. Production storage/authentication must be connected before accepting real identity documents.</div><p>Your KYC is now <b>Under Review</b>.</p><Link className="btn primary" href="/dashboard">View status</Link></>:<form onSubmit={submit}><p className="muted">Provide accurate information. Only upload documents you are authorized to submit.</p><div style={{display:'grid',gap:18,marginTop:24}}><label><span className="label">Full name</span><input className="input" required placeholder="Your full name"/></label><label><span className="label">Email</span><input className="input" type="email" required placeholder="you@example.com"/></label><label><span className="label">Document type</span><select className="input" required><option value="">Select</option><option>PAN Card</option><option>Passport</option><option>Driving Licence</option><option>Other government ID</option></select></label><div className="upload"><b>Upload identity document</b><p className="muted">PDF, JPG or PNG · Production version will use private object storage.</p><input type="file" accept="image/*,.pdf" required/></div><label><span className="label">Selfie (optional)</span><input className="input" type="file" accept="image/*"/></label><label className="row"><input type="checkbox" required/><span className="muted">I consent to verification and processing of the information I submit.</span></label><button className="btn primary" type="submit">Submit KYC</button></div></form>}</section></main>}
